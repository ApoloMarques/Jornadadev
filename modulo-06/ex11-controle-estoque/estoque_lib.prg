FUNCTION MostrarMenu()

    LOCAL cOpcao := ""

    CLS

    ? "==============================="
    ? "    CONTROLE DE ESTOQUE"
    ? "==============================="
    ?
    ? "1 - Cadastrar produto"
    ? "2 - Listar produtos"
    ? "3 - Entrada de estoque"
    ? "4 - Saida de estoque"
    ? "5 - Buscar produto"
    ? "6 - Relatorio"
    ? "0 - Sair"
    ?

    ACCEPT "Opcao: " TO cOpcao

RETURN cOpcao


FUNCTION CadastrarProduto(aProdutos)

    ? "Cadastrar Produto (em desenvolvimento)"

RETURN NIL


FUNCTION ListarProdutos(aProdutos)

    ? "Listar Produtos (em desenvolvimento)"

RETURN NIL


FUNCTION EntradaEstoque(aProdutos)

    ? "Entrada de Estoque (em desenvolvimento)"

RETURN NIL


FUNCTION SaidaEstoque(aProdutos)

    ? "Saida de Estoque (em desenvolvimento)"

RETURN NIL


FUNCTION BuscarProduto(aProdutos)

    ? "Buscar Produto (em desenvolvimento)"

RETURN NIL


FUNCTION Relatorio(aProdutos)

    ? "Relatorio (em desenvolvimento)"

RETURN NIL